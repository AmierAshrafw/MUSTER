# Local Claude Code settings (machine-specific, never commit)
.claude/settings.local.json

# Built MUSTER binary (rebuild via: go build -o muster.exe ./cmd/muster)
/muster.exe

# musterbench run scratch: redirect TMP/TEMP here so fixtures + the rebuilt
# muster.exe live inside the Defender-excluded repo (avoids per-spawn AV scans).
/.benchtmp/

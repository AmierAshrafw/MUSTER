# Plan review sidecar: 2026-08-13-test-speed-phase2-fixture.md

Reviewer: plan-reviewer subagent (opus). Reviewer executed the plan's code under Pester 6.0.1 / PS 5.1 before judging. 12 findings: 1 blocker, 6 warnings, 5 suggestions. All 12 ACCEPTED and applied to the plan.

## Verdicts

- **[B1] sh parity command false-green** — ACCEPT. Reviewer ran the fenced `bash` command: parent shell expands `$env:MUSTER_ENGINE` away, PowerShell continues past a non-terminating CommandNotFoundException and reruns the ps1 suite green while never touching sh. Fixed: Task 3 Step 5 now uses a single-quoted `-Command '$env:MUSTER_ENGINE="sh"; ...'` payload (literal under both bash and PowerShell parents), with an inline warning.
- **[W1] `$initBest` StrictMode crash + template leak on throw** — ACCEPT. `Measure-Object` over an empty set + `.Minimum` throws under StrictMode 2.0, discarding 3-5 min of measurement; `Remove-MusterFixture $template` was not in `finally`. Fixed: measurement loop wrapped in try/finally; explicit `$initRows.Count -eq 0` guard with a clear throw before any doc math.
- **[W2] template is per-test-file, not per-host-process** — ACCEPT. Reviewer's two-file Pester experiment proved a `$script:` cache in a dot-sourced helper rebuilds per test file (~12 builds/run, ~12 leaked TEMP dirs). Fixed: adoption-code comment and benchmark doc text now say "per Pester test file, ~12x per full suite run"; Task 4 Result templates match.
- **[W3] 292.2 s baseline is a per-file p50 sum, not a single-run wall time** — ACCEPT. Evidence: `Measure-Baseline.ps1` sums per-file p50s across nine separate hosts, root-level files only; `-Path tests` discovers 12+ containers. Fixed: Spec header note, Task 3 Step 4 labeling instruction, Task 4 Result templates state the method inline and forbid the naive comparison.
- **[W4] swap silently changes what Measure-Baseline.ps1 measures** — ACCEPT. Post-swap, its `fixture create+destroy` row pays the template build in sample 1 and times a bare copy afterwards; the script overwrites the committed baseline doc on rerun. Fixed: new Task 3 Step 2 adds a pointer comment above that row (comment only, baseline doc untouched); rerunning the baseline script listed in Out of scope.
- **[W5] lock check vacuous; real property is fixture-owned git dir** — ACCEPT. `*.lock` scan passes for all four strategies (verified empty); the checklist item "checkout lock under .git/" actually requires each fixture to own its git dir, which worktree fails (`--absolute-git-dir` resolves under the template). Fixed: contract now asserts `$a/.git` is a directory (disqualifies worktree by contract, not prose), lock scan runs on the owned git dir, Task 2 expectation names the worktree DISQUALIFIED outcome as expected. Benchmark restructured to still TIME disqualified strategies (spec says benchmark all four) while barring them from adoption.
- **[W6] missing Out of scope / Not yet specified sections** — ACCEPT. Sibling phase 0-1 plan carries both; added matching sections.
- **[S1] BeforeAll placement precedent** — ACCEPT. All 12 existing test files use top-level `BeforeAll`; plan's test now mirrors `tests/Harness.Tests.ps1:1` exactly (reviewer verified the original form also worked, so consistency-only).
- **[S2] fold contract test into Harness.Tests.ps1 instead of a 13th file** — ACCEPT. `tests/Harness.Tests.ps1:3-13` already asserts fixture shape and history; adding the contract `It` there avoids a new root-level file (which would also grow `Measure-Baseline.ps1`'s per-file enumeration). Plan rewritten: no `tests/Fixture.Tests.ps1`; Tasks 1 and 3 reference `Harness.Tests.ps1`.
- **[S3] fixture A leaks if creating B throws** — ACCEPT. Both `New` calls moved inside the try; finally guards with `if ($a)` / `if ($b)`; leftover-path checks guarded the same way.
- **[S4] clone-local drops the autocrlf guard** — ACCEPT. `New-MusterFixture` carries `-c core.autocrlf=false` deliberately (`tests/MusterFixture.ps1:24-28`); on autocrlf=true boxes a bare clone checks out different line endings. Fixed in the strategy and the clone-local adoption body.
- **[S5] clone-local leaves origin pointing at the leaked TEMP template** — ACCEPT. Reviewer confirmed the remote refs exist on a real local clone; this is exactly the spec's "stale .git content". Fixed: strategy runs `git remote remove origin`; contract now asserts `git remote` is empty for every strategy.

## Reviewer context

Reviewer read the plan, `tests/MusterFixture.ps1`, `tests/bench/Measure-Baseline.ps1`, `tests/Harness.Tests.ps1`, `tests/Status.Tests.ps1`, `tests/fast/*`, both spec docs, and the phase 0-1 plan; executed the strategies/contract verbatim from scratchpad (all four passed the then-current contract; cycle times init 0.805 s / copy 0.364 s / clone-local 0.692 s / worktree 0.337 s). Nothing was written to the repo by the reviewer.

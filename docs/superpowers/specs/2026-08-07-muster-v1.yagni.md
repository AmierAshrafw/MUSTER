# YAGNI sidecar: 2026-08-07-muster-v1.md

Auditor: yagni-guardian subagent (opus), 2026-08-07. Tier: SOFT. 2 findings.
Y4 scored against architecture.md open-items mandate - zero scope creep.
Verdicts by main thread. Both ACCEPTED and applied.

- [Y3] `bin/claim -Id` selector had zero callers in any specified flow; dragged
  two refusal rows with it.
  ACCEPT - selection is now always lowest-eligible-filename; dependency order
  is the only order that matters. Flag, step-5 branch, and both refusal rows
  cut (4.1, 8.4).
- [Y6] Fix `generation` computed twice: reviewer counts + writes, script
  recounts + refuses mismatch. Only the script count was load-bearing.
  ACCEPT - reviewer now stages a gen-less fix; `bin/done fail` computes g and
  stamps filename, `id`, `generation`, title on accept. Counting instruction,
  mismatch refusal, and refusal row cut (2.1, 2.2, 2.6, 4.3, 7.2, 7.3, 8.4).

Dropped by auditor (documented to avoid re-litigation): Y4 zero (staging/ is
delegated D11 mechanics), Y1 zero (Codex machinery within D16 allowance),
Y7 zero (staging refusal reachable via RECOVERY path), Y2/Y5 zero,
timeout_seconds kept (real consumer in spec example).

# Review sidecar: 2026-08-07-muster-v1.md

Reviewer: plan-reviewer subagent (opus), 2026-08-07.
Verdicts by main thread. 18 findings: 7 B / 7 W / 4 S. All ACCEPTED and applied.

## Blockers

- [B1] Claim-probe auto-files review/integration tasks (verify green before
  judgment happens); tier 1 never runs.
  ACCEPT - probe now gated on `type: impl|fix` AND prior-claim evidence in git
  history (4.1 step 9, marked [spec decision]).
- [B2] Generation-cap off-by-one: counting staging/ refused gen-2; cap became
  1 cycle, contradicting D11.
  ACCEPT - counting base excludes staging/ everywhere (2.2, 4.3 step 7, 7.2
  step 5); script also validates the staged fix's `generation` field.
- [B3] Blanket dirty-tree refusal deadlocked the D12 crash-recovery path.
  ACCEPT - check moved after task selection and scoped to the selected task's
  commit_paths (4.1 step 7); RECOVERY rewritten with explicit ordering and a
  do-not-stash warning.
- [B4] `done fail` unimplementable for integration (no `reviews`, seq 99
  reserved, no impl-id for cap).
  ACCEPT - integration fail gets its own contract: no fix task, straight to
  failed/, findings routed to the orchestrator (4.3, 7.4 step 5).
- [B5] Lint check 5 required `protected` on review/integration templates that
  legally omit it - shard would reject its own output.
  ACCEPT - check 5 scoped to impl/fix (2.6).
- [B6] RUNNER "never edit anything under tasks/" contradicted the mandatory
  notes file and staged fix.
  ACCEPT - rule narrowed to an explicit whitelist (RUNNER step 2).
- [B7] Cycled review kept its verify.log (attempt counter carried into round 2)
  and silently overwrote round-1 result.md.
  ACCEPT - `done fail` renames sidecars to `<id>.gen<g>.*` history; fresh log
  per round (4.3 step 8, section 3 folder contract).

## Warnings

- [W1] Verify-named files the task itself creates were forced into `protected`,
  making such tasks impossible to complete.
  ACCEPT - check 5 satisfied by `protected` OR `commit_paths` membership (2.6).
- [W2] `.gitkeep` made every folder literally non-empty; occupancy undefined.
  ACCEPT - occupancy counts task .md files only, defined once in 4.0.
- [W3] Status print sat behind two refusals; unhealthy boards printed nothing.
  ACCEPT - print moved to step 2, before all refusals (4.1).
- [W4] `claim_commit` used but never stored or derived.
  ACCEPT - derivation pinned in 4.3 precondition 1.
- [W5] staging/ had no lifecycle; stale orphans + lint-lite self-collision.
  ACCEPT - claim refuses on non-empty staging/ (4.1 step 4), refusal leaves
  the file for the live reviewer (4.3 step 6), lint-lite exempts the candidate
  (2.6), RECOVERY covers deletion.
- [W6] result.md status enum had no value for the cycled state.
  ACCEPT - `status: cycled` added (3.1).
- [W7] Review verify failure routed a reviewer into an impossible fix loop.
  ACCEPT - RUNNER step 3: on review/integration, verify failure = notes + stop.

## Suggestions

- [S1] RUNNER verdict parenthetical omitted integration. ACCEPT - added.
- [S2] `-Id` not-found behavior undefined. ACCEPT - refusal row added (8.4).
- [S3] Plan snapshot fate at close unspecified. ACCEPT - snapshot moves to
  archive/<plan-id>/ (section 1).
- [S4] Three different stop phrases; RUNNER keyed on one. ACCEPT - every
  terminal print now ends `Session over.`; RUNNER names it the only stop
  signal.
